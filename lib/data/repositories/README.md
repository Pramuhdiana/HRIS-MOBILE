# Repository Implementations

Tempat untuk repository implementations.

Setiap repository interface dari domain layer diimplementasikan di sini.
Contoh: `employee_repository_impl.dart`, `attendance_repository_impl.dart`