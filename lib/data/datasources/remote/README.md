# Remote Data Sources

Tempat untuk remote data sources (API calls).

Setiap feature memiliki remote data source sendiri.
Contoh: `employee_remote_datasource.dart`, `attendance_remote_datasource.dart`