# Local Data Sources

Tempat untuk local data sources (SharedPreferences, SQLite, Hive, dll).

Setiap feature memiliki local data source sendiri untuk caching.
Contoh: `employee_local_datasource.dart`, `attendance_local_datasource.dart`